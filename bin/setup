#! /bin/bash

env_name="qenv"
venv_path="$HOME/.local/share/envs/$env_name"

if [ ! -d "$venv_path" ]; then
  python -m venv "$venv_path"
fi

source "$venv_path/bin/activate"
python -m pip install --upgrade sysetup

sysetup
